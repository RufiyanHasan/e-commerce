# Core

Singleton services, guards, and interceptors. Import only once (e.g. in `AppConfig` or root injector).

- **services/** – app-wide services (e.g. CartService, AuthService)
- **guards/** – route guards (e.g. AuthGuard)
- **interceptors/** – HTTP interceptors (e.g. AuthInterceptor)
