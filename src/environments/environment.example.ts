// Copy this file to environment.ts and fill in your own keys.
// Never commit environment.ts — it is in .gitignore.
export const environment = {
  production: false,
  paypalClientId: 'REPLACE_WITH_YOUR_SANDBOX_CLIENT_ID',
};
